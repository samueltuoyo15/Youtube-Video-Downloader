services:
  - type: web
    name: youtube-video-downloader
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: YTDL_NO_UPDATE
        value: true
    routes:
      - type: rewrite
        source: '/'
        destination: '/'
